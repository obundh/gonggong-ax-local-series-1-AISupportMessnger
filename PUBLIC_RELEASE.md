# Public release boundary

이 폴더는 원본 개발 트리에서 공개 가능한 소스만 선별한 별도 배포본입니다.

## 제거 또는 제외한 범위

- 사용자 실명, 부서명과 로컬 계정명
- `heyu_workspace`: 대화, 속기, 임시파일, 사용자 출력물
- `outputs`, `tmp`: 테스트 결과, 로그, 스크린샷, 휴대용 산출물
- `data`: 법령·판례·EMP 원문, 번역 캐시, 인덱스와 기관 자료
- Git 추적 대상의 대용량 모델·런타임 빌드 캐시와 이미지 생성 가중치
- `node_modules`, ComfyUI, Python vendor
- 실존 인물 프로필 사진과 출처 확인이 끝나지 않은 문서 바이너리
- 로컬 LLM 설정, DGX 모델 매핑, API 인증값

## 유지한 범위

- 앱과 MCP 소스 코드
- 데이터 수집·파싱·안전 샘플 생성 스크립트
- 재현에 필요한 패키지 잠금 파일과 설정 예시
- 라이선스·저작권 점검 문서
- 기본 STT 번들 준비 스크립트와 라이선스 전문, 검증형 로컬 모델 파일 설치 소스

Whisper 실행기, `large-v3-turbo-q5_0`, Silero VAD의 실제 바이너리는 일반 Git에서 제외합니다. `npm run build`가 무시된 `vendor/stt-bundle` 빌드 캐시에 고정 자산을 준비하고 검증한 뒤 Windows 설치파일에만 포함합니다. `small-q5_1` 등 동봉되지 않은 모델은 앱에서 외부 URL을 호출하지 않고 사용자가 선택한 정확한 카탈로그 파일만 앱 데이터 폴더로 검증·설치합니다. 완성 설치파일은 소스 커밋이 아니라 별도 릴리스 자산으로 배포해야 합니다.

Series 4 포터블 ZIP도 일반 Git에서 제외합니다. 빌드 단계에서 공식 v4.1.1 자산의 크기·SHA-256·ZIP 구조를 확인해 `vendor/series4-bundle`에 준비하고 Windows 설치파일에만 포함합니다. 설치된 앱은 이 내장 자산만 사용하며 네트워크 다운로드 경로를 제공하지 않습니다.

## 공개 전 재확인

1. `npm run verify:public`을 실행합니다.
2. 새 문서·이미지·샘플의 본문과 메타데이터를 사람이 확인합니다.
3. `git status --ignored`로 로컬 데이터가 추적되지 않는지 확인합니다.
4. `git ls-files`에 모델, 데이터, 로그, 개인 설정이 없는지 확인합니다.
5. 프로젝트 자체 라이선스는 `UNLICENSED`로 표시되어 있으며 별도 허락 없이 사용·수정·재배포하지 않습니다. 제3자 고지는 `THIRD_PARTY_NOTICES.md`와 `third_party/licenses`를 기준으로 합니다.
6. 실제 배포 산출물은 소스 저장소와 별도로 라이선스·개인정보·실행 검증을 반복합니다.

## 의존성 배포 검사

2026-08-16에 운영 의존성을 다시 감사했습니다. 사용하지 않던 `pptxgenjs`와 그 전이 `image-size`를 제거하고, `sharp`, `uuid`, `brace-expansion`을 취약 버전 밖으로 갱신했습니다. `npm audit --omit=dev --audit-level=high`은 취약점 0건이며, Sharp 제한형 이미지 변환과 ExcelJS 저장·재열기 회귀도 통과했습니다. 배포 전에는 `npm run test:release-dependencies`와 운영 의존성 감사를 다시 실행해야 합니다.

자동 정규식 검사는 문맥상 개인정보와 이미지·문서 메타데이터를 완전히 판별하지 못합니다. 공개 직전 사람의 검토가 마지막 단계입니다.
